name: FixFlow CI/CD

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  backend-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -f backend/pom.xml clean package -DskipTests

  frontend-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
    - name: Install dependencies
      run: npm install --prefix frontend
    - name: Build Angular
      run: npm run build --prefix frontend -- --configuration production

  docker-verify:
    runs-on: ubuntu-latest
    needs: [backend-build, frontend-build]
    steps:
    - uses: actions/checkout@v4
    - name: Build Backend Docker Image (dry run)
      run: docker build -t fixflow-backend:test ./backend
    - name: Build Frontend Docker Image (dry run)
      run: docker build -t fixflow-frontend:test ./frontend

